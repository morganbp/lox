class Thing {
  init() {
    this.hei = "Hei på deg i utganspunktet";

  }
  getCallback() {
    fun localFunction() {
      print this.hei;
    }

    return localFunction;
  }
}
var ting = Thing();
ting.getCallback()();
ting.hei = "Hei på meg";
var callback = ting.getCallback();
callback();