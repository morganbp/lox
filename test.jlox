class Thing {
  getCallback() {
    fun localFunction() {
      print this.hei;
    }

    return localFunction;
  }
}
var ting = Thing();
ting.hei = "Hei på meg";
var callback = ting.getCallback();
callback();